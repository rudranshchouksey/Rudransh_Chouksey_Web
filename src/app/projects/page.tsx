import { ScrollProjectSlideshow } from "@/components/page/scroll-project-slideshow"

export default function Page() {

  return (
    <main className="min-h-screen w-full bg-[#0a0a0a] text-white antialiased">
      <ScrollProjectSlideshow />
    </main>
  );
}